class Solution {
    public int earliestTime(int[][] tasks) {
        int sum1=Integer.MAX_VALUE,sum2=0;
        for(int i=0;i<tasks.length;i++)
        {
            sum2=tasks[i][0]+tasks[i][1];
            sum1=Math.min(sum1,sum2);
        }
        return sum1;    
    }
}
