class Solution {
    public int mirrorDistance(int n) {
        if (n<=9) return 0;
        int rev=0,o=n;
        while(n!=0)
        {
            rev=(rev*10)+n%10;
            n/=10;
        }
        return Math.abs(o-rev);
    }
}
