class Solution {
    public int reverseDegree(String s) {
        s=s.toLowerCase();
     int rev=0;
     int sum=0;
     for(int i=0;i<s.length();i++)
     {
        rev=26-(s.charAt(i)-'a');
        sum+=rev*(i+1);
     }  
     return sum; 
    }
}
