class Solution {
    public int[] findDegrees(int[][] matrix) {
        int n=matrix[0].length;
        int []a=new int[n];
        for(int i=0;i<n;i++){
            int s=0;
            for(int j=0;j<n;j++)
                s+=matrix[i][j];
            a[i]=s;
        }
        return a;
    }
}
