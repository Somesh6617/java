class Solution {
    public long flowerGame(int n, int m) {
        int oddX = (n + 1) / 2;  
        int evenX = n / 2;  
        int oddY = (m + 1) / 2;  
        int evenY = m / 2;     
        long answer = (long) oddX * evenY + (long) evenX * oddY;
        return answer;
    }
}
