class Solution {
    public boolean checkGoodInteger(int n) {
        int s1=0,s2=0;
        while (n>0)
        {
            s1+=n%10;
            s2+=(n%10)*(n%10);
            n/=10;
        }
        return (s2-s1)>=50?true:false;
    }
}
