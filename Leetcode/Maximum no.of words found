class Solution {
    public int mostWordsFound(String[] sentences) {
        int mx=-1;
        for(String s:sentences)
        {
            String[] ss=s.split(" ");
            mx=Math.max(mx,ss.length);
        }
        return mx;
    }
}
