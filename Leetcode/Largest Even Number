class Solution {
    public String largestEven(String s) {
        int l=-1;
        for(int i=0;i<s.length();i++)
            {
                if(s.charAt(i)=='2')
                    l=i;
            }
        if(l==-1) return "";
        return s.substring(0,l+1);
    }
}
