class Solution {
    public int xorOperation(int n, int start) {
        int x=0;
        if(n==1) return start;
        for(int i=0;i<n;i++)
        {
            x^=start;
            start+=2;
        }
        return x;
    }
}
